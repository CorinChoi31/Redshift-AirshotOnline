/// @function camera_3d_look_to(camera, x, y, z)
/// @param camera
/// @param x
/// @param y
/// @param z

function camera_3d_look_to(_camera, _x, _y, _z)
{
	_camera.camera_xto = _x;
	_camera.camera_yto = _y;
	_camera.camera_zto = _z;
}
